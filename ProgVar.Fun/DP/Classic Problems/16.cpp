#include <bits/stdc++.h>
using namespace std;
typedef long long int ll;

int main(){
    int l; cin >> l;
    while (l != 0) {
        int n; cin >> n;
        vector<int> arr;
        for (int i = 0; i < n; i++) {
            int ai; cin >> ai;
            arr.push_back(ai);
        }

        




        cin >> l;
    }
}
#include <bits/stdc++.h>
using namespace std;
typedef long long int ll;

int main() {
    int n; cin >> n;
    vector<ll> arr;
    for (int i = 0; i < n; i++) {
        ll x; cin >> x;
        arr.push_back(x);
    }

    
}
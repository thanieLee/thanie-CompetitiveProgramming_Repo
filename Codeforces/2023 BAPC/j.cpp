#include <bits/stdc++.h>
using namespace std;
typedef long long int ll;
#define MOD 1000000007ll


using cd = complex<double>;
const double PI = acos(-1);

int solve(int u, int v, vector<vector<int>> &adj) {
    if (adj[u].size() == 0) {
        if (v <= 1) return 1;
        return 0;
    } else {
        int ans = 1;
        for (int)
    }
}

int main(){
    ll n; cin >> n;
    

}